name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests 
      run: |
        pytest tests_agents.py
        pytest tests_orchestrator.py

    - name: Run flake8 for linting
      run: |
        pip install flake8
        flake8 .

    - name: Run pytest for testing
      run: |
        pip install pytest
        pytest
